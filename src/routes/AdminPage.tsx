/**
 * Admin shell placeholder. Implemented in Phase 4 (list management, TMDB search/add, reorder,
 * discussion-topic generation/approval). Access is enforced by SWA via staticwebapp.config.json
 * (`admin` role on /admin/* and /api/admin/*).
 */
export default function AdminPage() {
  return (
    <section>
      <h1 className="page-title">Admin</h1>
      <p className="muted">Admin tools are added in Phase 4.</p>
    </section>
  );
}
